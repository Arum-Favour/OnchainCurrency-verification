# NPM Configuration for Currency Verification App
# This helps npm understand the correct workspace root

# Set the workspace root to current directory
workspace-root=.

# Disable package-lock.json in parent directories
package-lock=false
